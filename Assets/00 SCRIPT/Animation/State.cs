using UnityEngine;

public class State 
{
    public enum PlayerState
    {
        run,
        idle,
        jump
    }
    public enum EnemyState
    {
        run,
        idle
    }

}
